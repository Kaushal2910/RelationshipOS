# Copy to .env and fill from Supabase → Project Settings → API.
# These are PUBLIC client values (safe in the bundle; RLS is the security boundary).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-REF.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=sb_publishable_xxx_or_anon_jwt
